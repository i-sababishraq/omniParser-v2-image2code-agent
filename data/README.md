# Data directory

This folder can hold local sample images for android/ios/web testing, but it should not be committed with large datasets. Use `.gitignore` to exclude real data and only keep small illustrative samples or document how to fetch data.
